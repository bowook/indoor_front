import 'package:flutter/material.dart';

class VisitorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('방문객 화면'),
      ),
      body: Center(
        child: Text('방문객 화면입니다'),
      ),
    );
  }
}